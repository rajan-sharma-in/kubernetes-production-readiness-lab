# Basic Deployment Example

This directory is reserved for a minimal Kubernetes deployment example.

Suggested contents:

- `deployment.yaml`
- `service.yaml`
- `configmap.yaml`

Use it to demonstrate a clean baseline workload with sensible readiness settings.
