# Helm Chart Example

This directory is reserved for a starter Helm chart.

Suggested contents:

- `Chart.yaml`
- `values.yaml`
- `templates/`

Use it to demonstrate chart structure, values management, and release validation.
